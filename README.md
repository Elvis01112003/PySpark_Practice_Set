# PySpark Practice

A hands-on repository for learning and practicing **PySpark** and **Apache Spark** concepts through notebooks and practical exercises.

## Topics

### PySpark Basics

* SparkSession
* DataFrames
* DataFrame Schema
* DataFrame API
* PySpark Functions
* Columns and Expressions

### DataFrame Operations

* `select()`
* `filter()`
* `where()`
* `withColumn()`
* `drop()`
* `distinct()`
* `dropDuplicates()`
* `orderBy()`
* `groupBy()`
* `agg()`
* `join()`

### PySpark Functions

* `col()`
* `expr()`
* `alias()`
* `cast()`
* `sum()`
* `avg()`
* `min()`
* `max()`
* `count()`

### Spark Concepts

* Transformations
* Actions
* Lazy Evaluation
* DAG
* Partitions
* Shuffles
* Caching
* Spark Execution

### Data Processing

* Reading CSV
* Reading JSON
* Writing Data
* Write Modes
* Data Partitioning
* Handling Null Values
* Data Type Conversion

### Advanced Topics

* Window Functions
* User Defined Functions
* Join Optimization
* Shuffle Optimization
* Adaptive Query Execution
* Spark SQL
* Delta Lake
* Z-Ordering
* Performance Optimization

## Learning Approach

The repository contains practical examples and exercises to understand how PySpark works and how its APIs are used for data processing.

```text
Learn
  ↓
Practice
  ↓
Experiment
  ↓
Understand
  ↓
Document
```

## Repository Structure

```text
pyspark-practice/
│
├── notebooks/
│   ├── dataframe/
│   ├── transformations/
│   ├── aggregations/
│   ├── joins/
│   ├── window-functions/
│   ├── spark-sql/
│   └── ...
│
├── data/
│
└── README.md
```

## Progress

* [x] SparkSession
* [x] Creating DataFrames
* [x] DataFrame API
* [x] Selecting and filtering
* [x] `col()` and `expr()`
* [x] `alias()`
* [x] `cast()`
* [x] Aggregations
* [x] Reading and writing data
* [ ] Joins
* [ ] Window Functions
* [ ] Partitions and Shuffles
* [ ] Caching
* [ ] Spark SQL
* [ ] Delta Lake
* [ ] Spark Optimization

## Purpose

This repository is maintained as a **personal PySpark practice and learning project**, with the goal of developing a strong foundation in Apache Spark and data engineering.
